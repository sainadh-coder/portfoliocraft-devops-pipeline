apiVersion: v1
kind: Service
metadata:
  name: portfoliocraft-service
spec:
  type: NodePort
  selector:
    app: portfoliocraft
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
